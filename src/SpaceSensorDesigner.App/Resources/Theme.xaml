<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">

    <!-- ===== Palette (warm "Butlr" tones) ===== -->
    <Color x:Key="AccentColor">#F2726F</Color>
    <SolidColorBrush x:Key="WindowBrush"     Color="#F1EEE8" />
    <SolidColorBrush x:Key="PanelBrush"      Color="#FFFFFF" />
    <SolidColorBrush x:Key="SidebarBrush"    Color="#F7F5F0" />
    <SolidColorBrush x:Key="RailBrush"       Color="#FFFFFF" />
    <SolidColorBrush x:Key="BorderBrush2"    Color="#ECE7DE" />
    <SolidColorBrush x:Key="TextBrush"       Color="#33302B" />
    <SolidColorBrush x:Key="TextMutedBrush"  Color="#948C7E" />
    <SolidColorBrush x:Key="AccentBrush"     Color="#F2726F" />
    <SolidColorBrush x:Key="AccentSoftBrush" Color="#FBE9E5" />
    <SolidColorBrush x:Key="IndigoBrush"     Color="#5B57D6" />
    <SolidColorBrush x:Key="CoveredBrush"    Color="#6FC7B6" />
    <SolidColorBrush x:Key="PartialBrush"    Color="#F3C46B" />
    <SolidColorBrush x:Key="UncoveredBrush"  Color="#EE9C7E" />

    <LinearGradientBrush x:Key="AccentGradientBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#F98A82" Offset="0" />
        <GradientStop Color="#F2726F" Offset="1" />
    </LinearGradientBrush>

    <!-- Soft elevation shadows -->
    <DropShadowEffect x:Key="SoftShadow" BlurRadius="26" ShadowDepth="3" Direction="270"
                      Color="#4A4033" Opacity="0.14" />
    <DropShadowEffect x:Key="CardShadow" BlurRadius="20" ShadowDepth="2" Direction="270"
                      Color="#4A4033" Opacity="0.12" />

    <!-- Floating card (white, rounded, soft shadow) used for the overlay panels -->
    <Style x:Key="FloatingCard" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource PanelBrush}" />
        <Setter Property="CornerRadius" Value="16" />
        <Setter Property="Effect" Value="{StaticResource CardShadow}" />
    </Style>

    <!-- ===== Text styles ===== -->
    <Style x:Key="SectionHeader" TargetType="TextBlock">
        <Setter Property="FontSize" Value="11" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}" />
        <Setter Property="Margin" Value="2,10,0,6" />
    </Style>

    <Style x:Key="FieldLabel" TargetType="TextBlock">
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}" />
        <Setter Property="Margin" Value="0,8,0,2" />
    </Style>

    <Style x:Key="TitleText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
    </Style>

    <!-- ===== Card border ===== -->
    <Style x:Key="Card" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource PanelBrush}" />
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush2}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="Padding" Value="10" />
    </Style>

    <!-- ===== Left-palette tool button (RadioButton, exclusive) ===== -->
    <Style x:Key="ToolButton" TargetType="RadioButton">
        <Setter Property="Width" Value="56" />
        <Setter Property="Height" Value="54" />
        <Setter Property="Margin" Value="0,3" />
        <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Grid>
                        <Border x:Name="Bd" CornerRadius="12" Background="Transparent" Margin="2,0" />
                        <Border x:Name="Bar" Width="3" HorizontalAlignment="Left" Margin="-4,10"
                                CornerRadius="2" Background="Transparent" />
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#F0F3F9" />
                            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentSoftBrush}" />
                            <Setter TargetName="Bar" Property="Background" Value="{StaticResource AccentBrush}" />
                            <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Tool icon: a filled Path that inherits the button's Foreground (tints on select/hover) -->
    <Style x:Key="ToolIcon" TargetType="Path">
        <Setter Property="Width" Value="21" />
        <Setter Property="Height" Value="21" />
        <Setter Property="Stretch" Value="Uniform" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="Fill" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType=RadioButton}}" />
    </Style>

    <!-- ===== Toolbar button (Button) ===== -->
    <Style x:Key="ToolbarButton" TargetType="Button">
        <Setter Property="Height" Value="34" />
        <Setter Property="Padding" Value="12,0" />
        <Setter Property="Margin" Value="3,0" />
        <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" CornerRadius="8" Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentSoftBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.4" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===== Accent (primary) button ===== -->
    <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource ToolbarButton}">
        <Setter Property="Background" Value="{StaticResource AccentBrush}" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" CornerRadius="8" Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#E85F5C" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.4" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===== Segmented radio (2D / 3D) ===== -->
    <Style x:Key="SegRadio" TargetType="RadioButton">
        <Setter Property="Width" Value="42" />
        <Setter Property="Height" Value="26" />
        <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Border x:Name="Bd" CornerRadius="6" Background="Transparent">
                        <TextBlock Text="{TemplateBinding Content}" HorizontalAlignment="Center"
                                   VerticalAlignment="Center" FontWeight="SemiBold" FontSize="12" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource PanelBrush}" />
                            <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===== Inputs ===== -->
    <Style TargetType="TextBox">
        <Setter Property="Height" Value="30" />
        <Setter Property="Padding" Value="8,0" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush2}" />
        <Setter Property="Background" Value="{StaticResource PanelBrush}" />
        <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="Height" Value="30" />
        <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
    </Style>

    <!-- ===== Window caption buttons (Segoe MDL2 glyphs) ===== -->
    <Style x:Key="CaptionButton" TargetType="Button">
        <Setter Property="Width" Value="46" />
        <Setter Property="Height" Value="34" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}" />
        <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
        <Setter Property="FontSize" Value="10" />
        <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#12000000" />
                            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="CloseButton" TargetType="Button" BasedOn="{StaticResource CaptionButton}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="#E81123" />
                            <Setter Property="Foreground" Value="White" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===== Round icon button (floating bottom toolbar) ===== -->
    <Style x:Key="RoundIconButton" TargetType="Button">
        <Setter Property="Width" Value="36" />
        <Setter Property="Height" Value="36" />
        <Setter Property="Margin" Value="2,0" />
        <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="FontSize" Value="15" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" CornerRadius="10" Background="{TemplateBinding Background}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource SidebarBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.35" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Small pill for a metric value (e.g. distance rows) -->
    <Style x:Key="ValuePill" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource SidebarBrush}" />
        <Setter Property="CornerRadius" Value="7" />
        <Setter Property="Padding" Value="8,3" />
    </Style>

    <!-- ===== Tool icon geometries (filled, EvenOdd) ===== -->
    <GeometryGroup x:Key="IconSelect" FillRule="Nonzero">
        <PathGeometry Figures="M5,3 L5,19 L9.2,14.8 L12,20 L14.2,19 L11.4,13.9 L17,13.9 Z" />
    </GeometryGroup>
    <GeometryGroup x:Key="IconWall" FillRule="EvenOdd">
        <RectangleGeometry Rect="3,6 16,10" />
        <RectangleGeometry Rect="3,10.4 16,0.9" />
        <RectangleGeometry Rect="10.6,6 0.9,4.4" />
        <RectangleGeometry Rect="6.6,11.3 0.9,4.7" />
        <RectangleGeometry Rect="14.6,11.3 0.9,4.7" />
    </GeometryGroup>
    <GeometryGroup x:Key="IconDoor" FillRule="EvenOdd">
        <PathGeometry Figures="M6,3 H15 V19 H6 Z" />
        <RectangleGeometry Rect="12,10 1.4,1.6" />
    </GeometryGroup>
    <GeometryGroup x:Key="IconWindow" FillRule="EvenOdd">
        <RectangleGeometry Rect="3,4 16,16" />
        <RectangleGeometry Rect="5,6 5.2,6" />
        <RectangleGeometry Rect="11.8,6 5.2,6" />
        <RectangleGeometry Rect="5,14 5.2,4" />
        <RectangleGeometry Rect="11.8,14 5.2,4" />
    </GeometryGroup>
    <GeometryGroup x:Key="IconFurniture" FillRule="Nonzero">
        <PathGeometry Figures="M4,8 H18 V15 H16 V13 H6 V15 H4 Z" />
        <RectangleGeometry Rect="4,14 2.4,3" />
        <RectangleGeometry Rect="15.6,14 2.4,3" />
    </GeometryGroup>
    <GeometryGroup x:Key="IconSensor" FillRule="EvenOdd">
        <EllipseGeometry Center="11,11" RadiusX="8" RadiusY="8" />
        <EllipseGeometry Center="11,11" RadiusX="4.4" RadiusY="4.4" />
        <EllipseGeometry Center="11,11" RadiusX="1.8" RadiusY="1.8" />
    </GeometryGroup>
    <GeometryGroup x:Key="IconRoom" FillRule="EvenOdd">
        <RectangleGeometry Rect="3,4 16,14" />
        <RectangleGeometry Rect="6,7 10,8" />
    </GeometryGroup>

</ResourceDictionary>
