namespace SpaceSensorDesigner.App.Services;

/// <summary>Static product identity used across the shell (title bars, About, installer, update check).</summary>
public static class AppInfo
{
    public const string ProductName = "SpaceSensor Designer";
    public const string Publisher = "SpaceSensor";
    public const string Version = "1.0.0";

    /// <summary>The document file extension (without the dot) this app owns.</summary>
    public const string DocumentExtension = "spacedesign";
}
